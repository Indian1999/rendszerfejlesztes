@page "/projects"
@using ClassLibrary.Models
@inject ILogger<Projects> Logger
@attribute [StreamRendering]

<PageTitle>Project</PageTitle>

<h1>Projects</h1>
<button @onclick="ButtonClick">Button</button>

@if (project == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Type</th>
                <th>Desctiption</th>
            </tr>
        </thead>
        <tbody>
                <tr>
                    <td>@project.id</td>
                    <td>@project.name</td>
                    @* <td>@project.type.name</td> *@
                    <td>@project.description</td>
                </tr>
        </tbody>
    </table>
}

@code {
    private Project[]? projects;
    private Project project;
    protected override async Task OnInitializedAsync()
    {
        try
        {
            HttpClient httpClient = new HttpClient();
            HttpResponseMessage response = await httpClient.GetAsync("http://localhost:5005/api/Project/1");
            if (response.IsSuccessStatusCode)
            {
                project = await response.Content.ReadFromJsonAsync<Project>();
                Logger.LogWarning("Succesful api reguest!");
            }
            else
            {
                Logger.LogWarning("Unsuccesful api reguest!");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine(ex.Message);
            Logger.LogWarning("api failed: " + ex.Message);
        }
    }


    private void ButtonClick()
    {
        Logger.LogWarning("Someone has clicked me!");
    }
}